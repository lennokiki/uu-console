import { TLocalSizeItem } from '@/types';
import { micro, tiny, small, medium, big, large, huge } from './modules';

const sizes: Record<string, TLocalSizeItem> = {
  micro,
  tiny,
  small,
  medium,
  big,
  large,
  huge,
};

export default sizes;
